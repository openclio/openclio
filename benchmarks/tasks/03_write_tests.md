Write table-driven Go tests for a helper named `truncateLines(input string, max int) string` with cases for:
- empty input
- max larger than line count
- exact match
- truncation with remainder indicator
