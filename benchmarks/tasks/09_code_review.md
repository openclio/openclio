Review this pseudo PR change for risks:

- Added browser automation tool that keeps a persistent page object.
- Added cron run mode that can reuse shared sessions.
- Added dynamic MCP server startup from config.

Return findings ordered by severity and suggest missing tests.
